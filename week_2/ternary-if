#include <iostream>
using namespace std;
int main()
{
	string textsize{ "small" };
		(textsize == "small") ? cout << "hello\n" : cout << "HELLO\n";
		//ternary if
		//(xxx== "xxx") ? xxx todorhoiloltiin tsegnii omnoh tal ni unen bh  : xxx todorhoiloltiin tsegnii ard tal ni hudal bh
}
